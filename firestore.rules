rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Require authenticated Firebase user for all document access.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
